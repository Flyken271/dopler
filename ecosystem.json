{
  "apps" : [{
    "name"      : "APP",
    "script"    : "yarn start",
    "env": {
      "NODE_ENV": "production"
    }
   }],
  "deploy": {
    "user" : "flyken",
    "host" : ["209.208.63.24"],
    "key": "~/.ssh/deploy_rsa.pub",
    "ref"  : "origin/master",
    "repo" : "git@gitlab.com:Flyken271/dopler.git",
    "path" : "/var/www/app",
    "post-deploy" : "yarn && pm2 startOrRestart ecosystem.json --name dopler"
  }
}